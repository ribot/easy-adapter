apply plugin: 'android'

android {
    compileSdkVersion 19
    buildToolsVersion "19.0.3"

    defaultConfig {
        minSdkVersion 7
        targetSdkVersion 19
        versionCode 2
        versionName "1.1"
    }
}

dependencies {
    compile project(":easyadapter")
}
